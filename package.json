{
  "name": "@moysklad/js-sdk-widget",
  "version": "0.1.0",
  "description": "Widget SDK build setup",
  "main": "dist/widget.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rollup -c rollup.config.mjs"
  },
  "devDependencies": {
    "@rollup/plugin-terser": "0.4.4",
    "rollup": "4.30.0"
  }
}
